<h1>Task Manager</h1>

<%# check_box_tag %>

<ul>
  <% @tasks.each do |t| %>
    <li><%= t.completed %> <%= link_to t.title, task_path(t.id) %></li>
  <% end %>
</ul>

<%= link_to "Add a new task", tasks_new_path %>
